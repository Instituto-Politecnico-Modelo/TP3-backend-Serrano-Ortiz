spring.application.name=OrtizSerranoTP3

# ──────────────────────────────────────────────────────────────────────────────
# Base de datos MySQL
# ──────────────────────────────────────────────────────────────────────────────
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/Decanato-Ortiz-Serrano?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# IMPORTANTE: definir DB_USERNAME y DB_PASSWORD como variables de entorno en producción.
# Nunca commitear credenciales reales en este archivo.
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:alumnoipm}

# JPA/Hibernate
# ⚠️  En producción usar 'validate' o 'none' — 'update' puede alterar el esquema automáticamente.
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
# ⚠️  En producción poner 'false' — loguear SQL puede exponer datos sensibles.
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=never

# ──────────────────────────────────────────────────────────────────────────────
# H2 Console (deshabilitado en producción MySQL)
# ──────────────────────────────────────────────────────────────────────────────
spring.h2.console.enabled=false

# Puerto del servidor
server.port=8080

# Configuración JWT
# En producción, definir la variable de entorno JWT_SECRET con un valor secreto seguro.
# El valor por defecto SOLO es válido para desarrollo local.
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970337336763979244226452948404D635166546A576E5A7234753778214125442A}
jwt.expiration=${JWT_EXPIRATION_MS:86400000}

# Swagger / OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true

# ──────────────────────────────────────────────────────────────────────────────
# Configuración de Filas Virtuales + Rate Limiter (Issue #26)
# ──────────────────────────────────────────────────────────────────────────────

# Rate Limiter: máximo de intentos de inscripción por usuario por ventana
inscripcion.ratelimit.max-intentos=5
inscripcion.ratelimit.ventana-segundos=60

# Semáforo por materia: máximo de transacciones DB concurrentes
# permits=1 garantiza serialización completa → evita sobrecupos bajo race conditions
inscripcion.semaforo.permisos=1
inscripcion.semaforo.timeout-ms=10000

# Cola virtual: horas que dura un turno antes de expirar
cola.expiracion.horas=48

# Tarea de limpieza de turnos expirados (en milisegundos, default 60s)
cola.limpieza.delay-ms=60000

# Metadatos de la aplicación (usados en emails)
app.nombre=Sistema de Decanato IPM
# ⚠️  IMPORTANTE: en producción definir la variable de entorno APP_URL con la URL real
# para que los links en los emails de bienvenida no apunten a localhost.
# Ejemplo: APP_URL=https://decanato.ipm.edu.ar
app.url=${APP_URL:http://localhost:5173}

# Orígenes permitidos para CORS (frontend).
# En producción, definir la variable de entorno APP_CORS_ALLOWED_ORIGINS con la URL real.
# Ejemplo: APP_CORS_ALLOWED_ORIGINS=https://decanato.ipm.edu.ar
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# ──────────────────────────────────────────────────────────────────────────────
# Configuración de Email (correo institucional)
# ──────────────────────────────────────────────────────────────────────────────
# En desarrollo (H2) el envío falla silenciosamente y las credenciales se
# registran en el log. Para habilitar el envío real, completar estas props.

# ── Gmail / Google Workspace ──────────────────────────────────────────────────
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=noreply@decanato.edu.ar
# spring.mail.password=TU_APP_PASSWORD_GOOGLE
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=true

# ── Outlook / Microsoft 365 ───────────────────────────────────────────────────
# spring.mail.host=smtp.office365.com
# spring.mail.port=587
# spring.mail.username=noreply@decanato.edu.ar
# spring.mail.password=TU_PASSWORD
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# ── SMTP institucional genérico ───────────────────────────────────────────────
# spring.mail.host=mail.ipm.edu.ar
# spring.mail.port=587
# spring.mail.username=noreply@ipm.edu.ar
# spring.mail.password=TU_PASSWORD
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.default-encoding=UTF-8
